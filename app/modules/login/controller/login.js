define(function(require) {

	var module = require('module!@');

	module.controller('login.controllers.login', [
		'$scope',
		function(
			$scope
			) {

			angular.extend($scope, {

			});
		}
	]);
});